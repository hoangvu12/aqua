# Dev-only: point the relay endpoints (/pair, /agent WS) at the local mock
# server (`bun run mock` on :9912) so `vite dev` keeps hot reload while speaking
# the real relay protocol. Loaded only in dev; production stays same-origin.
VITE_RELAY_ORIGIN=http://127.0.0.1:9912
